//Recursive function to find sum till N
#include<bits/stdc++.h>
using namespace std;
int sumN(int n)
{
    if(n==0)
    return 0;
    int sum;
    sum=n+sumN(n-1);
    return sum;
}
int main()
{
    int n;
    cin>>n;
    cout<<sumN(n);
    return 0;
}
